---
import { calculateReadingTime } from "@utils/calculate-read-time";
import { getPosts } from "@/utils/get-posts";
import { normalizeTag } from "@/utils/normalize-tag";
import Tag from "@/components/tag.astro";
import ChevronIcon from "@/components/ui/chevron-icon.astro";
import { Picture } from "astro:assets";
import clsx from "clsx";

type Props = {
  title: string;
};

const { title } = Astro.props;

const allBlogPosts = await getPosts();
const currPost = allBlogPosts.find((post) => post.data.title === title);
const readTime = calculateReadingTime(currPost?.body || "");
const words = currPost?.body ? currPost.body.split(/\s+/).length : 0;

const publishDate = new Intl.DateTimeFormat("pl-PL", {
  year: "numeric",
  month: "long",
  day: "numeric",
}).format(
  currPost?.data.pubDate ? new Date(currPost.data.pubDate) : new Date(),
);

const updateDate = currPost?.data.updatedDate
  ? new Intl.DateTimeFormat("pl-PL", {
      year: "numeric",
      month: "long",
      day: "numeric",
    }).format(new Date(currPost.data.updatedDate))
  : null;

const tags = currPost?.data.tags || [];

const sortedTags = tags.sort((a, b) =>
  a.localeCompare(b, undefined, { sensitivity: "base" }),
);

const isLongTitle = title.length > 60;
---

<div class="lg:w-[85ch] lg:mx-auto">
  <a
    id="back-link"
    href="/blog"
    class="text-foreground-secondary hover:text-foreground group inline-flex w-fit items-center gap-2 text-sm transition-colors"
  >
    <ChevronIcon
      size={16}
      direction="left"
      customStyles="transition-transform group-hover:-translate-x-1"
    />
    Inne posty
  </a>
</div>

<script>
  function initLink() {
    const backLink = document.getElementById("back-link");
    if (!backLink) return;

    backLink.onclick = (e) => {
      const referrer = document.referrer;
      let isSameOrigin = false;
      let referrerPathname = "";

      try {
        if (referrer) {
          const referrerUrl = new URL(referrer);
          isSameOrigin = referrerUrl.origin === window.location.origin;
          referrerPathname = referrerUrl.pathname;
        }
      } catch (err) {}

      if (isSameOrigin) {
        e.preventDefault();

        if (referrerPathname === "/") {
          window.location.href = "/blog";
        } else {
          history.back();
        }
      }
    };
  }

  document.addEventListener("DOMContentLoaded", initLink);
  document.addEventListener("astro:after-swap", initLink);
</script>
<header class="relative mx-auto flex w-full lg:max-w-[85ch] flex-col gap-6">
  <div class="flex flex-col gap-6 md:flex-row-reverse md:items-start md:gap-10">
    <div
      class="flex flex-1 flex-col gap-4"
      transition:name={`blog-header-${currPost?.id}`}
    >
      <h1
        class={clsx(
          "max-w-[40rem] text-2xl leading-tight font-bold text-balance md:text-3xl ",
          {
            "lg:text-4xl": !isLongTitle,
            "lg:text-3xl": isLongTitle,
          },
        )}
      >
        {title}
      </h1>

      <div
        class="text-foreground-secondary flex flex-wrap items-center gap-x-2 gap-y-1 text-sm"
      >
        <time
          itemprop="datePublished"
          datetime={new Date(currPost?.data.pubDate || "").toISOString()}
        >
          {publishDate}
        </time>

        {
          currPost?.data.updatedDate && (
            <>
              <span class="text-accent">&bull;</span>
              <time
                itemprop="dateModified"
                datetime={new Date(currPost.data.updatedDate).toISOString()}
              >
                aktualizacja {updateDate}
              </time>
            </>
          )
        }

        <span class="text-accent">&bull;</span>
        <span>{readTime} min. czytania</span>
        <span class="text-accent">&bull;</span>
        <span>{words} słów</span>
      </div>

      {
        sortedTags.length > 0 && (
          <div class="flex flex-wrap items-center gap-1.5 pt-1">
            {sortedTags.map((tag) => (
              <Tag href={`/blog/tag/${normalizeTag(tag)}`}># {tag}</Tag>
            ))}
          </div>
        )
      }
    </div>

    {
      currPost?.data.heroImage && currPost.id && (
        <Picture
          formats={["avif", "webp"]}
          src={currPost.data.heroImage}
          transition:name={`blog-image-${currPost.id}`}
          width={350}
          height={350}
          alt={""}
          pictureAttributes={{
            class: "hidden md:block",
          }}
          class="aspect-square mx-auto md:w-48 shrink-0 rounded-xl object-cover shadow-lg border border-foreground/10"
          priority={true}
        />
      )
    }
  </div>

  <hr class="border-border/50" />
</header>
